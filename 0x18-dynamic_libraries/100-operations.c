#include "task_adv_one.h"
/**
 * add - funtions for aritmetic sum
 * @a: value one
 * @b: value two
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - funtions for aritmetic sub
 * @a: value one
 * @b: value two
 * Return: result
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 * mul - funtions for aritmetic mul
 * @a: value one
 * @b: value two
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - funtions for aritmetic div
 * @a: value one
 * @b: value two
 * Return: result
 */
int div(int a, int b)
{
	return (a / b);
}

/**
 * mod - funtions for aritmetic module
 * @a: value one
 * @b: value two
 * Return: result
 */
int mod(int a, int b)
{
	return (a % b);
}
